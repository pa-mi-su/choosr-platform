# Server-side provider credentials for local Edge Function development only.
# Copy to supabase/.env.local. Never place these values in the mobile .env file.
TMDB_API_READ_TOKEN=
GOOGLE_PLACES_API_KEY=
