# Development Firebase configuration

Download `GoogleService-Info.plist` for `com.pamisu.choosr.dev` and place it
in this directory. The plist is intentionally ignored until the development
Firebase project is provisioned.
