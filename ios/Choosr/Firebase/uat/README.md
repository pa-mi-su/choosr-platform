# UAT Firebase configuration

Download `GoogleService-Info.plist` for `com.pamisu.choosr.uat` and place it
in this directory. The plist is intentionally ignored until the UAT Firebase
project is provisioned.
