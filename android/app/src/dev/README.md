# Development Firebase configuration

Download the Android `google-services.json` for `com.pamisu.choosr.dev` and
place it in this directory. The file is intentionally ignored until the
development Firebase project is provisioned.
