# Production Firebase configuration

The production flavor currently uses `android/app/google-services.json`.
Before public launch, replace it with the client configuration downloaded from
the dedicated production Firebase project.
