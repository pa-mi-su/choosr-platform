# UAT Firebase configuration

Download the Android `google-services.json` for `com.pamisu.choosr.uat` and
place it in this directory. The file is intentionally ignored until the UAT
Firebase project is provisioned.
